name: Deploy Supabase Edge Functions

on:
  push:
    branches:
      - main
    paths:
      - 'supabase/functions/**'
      - 'supabase/config.toml'

jobs:
  notify:
    runs-on: ubuntu-latest
    
    steps:
      - name: Deployment Note
        run: echo "📝 Supabase Edge Functions are deployed manually via Supabase dashboard when needed"
